package sokoban;

public class Controller {
	/**
	 * this is where the code to take user input, and interact with the model/s then relay information back to the view  
	 */
	
	
	/*
	 * test level
	 * 
	 * wwwwwwww
	 * w..g...w
	 * w......w
	 * w......w
	 * w...p..w
	 * wwwwwwww
	 * 
	 */
	
	
	
	
	
	
	
	
	
	IView myView;
	public Controller(IView theView) {
		this.myView = theView;
	}
	
	public void go() {
		this.myView.start();
		this.myView.stop();
	}
}
